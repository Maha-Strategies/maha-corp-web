{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://www.mahastrategies.com/conformance/x402-v2/declaration-integrity.schema.json",
  "title": "Proposed x402 declaration-integrity extension",
  "type": "object",
  "additionalProperties": false,
  "required": ["declarationDigest", "metadataVersion", "canonicalResource"],
  "properties": {
    "declarationDigest": {
      "type": "string",
      "pattern": "^sha256:[a-f0-9]{64}$",
      "description": "SHA-256 of the RFC 8785-style canonical discovery declaration after removing extensions.declaration-integrity."
    },
    "metadataVersion": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}(?:[.+-][A-Za-z0-9._-]+)?$",
      "description": "Seller-controlled human-readable revision. The digest, not this value, is authoritative."
    },
    "canonicalResource": {
      "type": "string",
      "format": "uri",
      "pattern": "^https://",
      "description": "Canonical HTTPS resource URL with no fragment."
    }
  }
}
