# Epistemic Clearing Expansion — Batch 1

Prepared: 2026-09-05  
State: prepared, not built or deployed  
Build gate: withhold every Next.js and Vercel build until at least 1,500 site pages are prepared and the operator explicitly authorizes the build.

## Delivered

Batch 1 prepares 100 finite routes and 500 bounded question-and-answer pairs:

| Lane | Routes | Purpose |
| --- | ---: | --- |
| Book-concept machine applications | 40 | Apply boundary, resilience, identity, and governance concepts to implemented machine controls without treating the books as technical evidence. |
| Tamil religion evidence clearing | 30 | Separate primary text, translation, commentary, historical inference, reception, and theology across methods, lineages, and divine names. |
| Astrology infrastructure | 20 | Preserve calculation provenance and test output sensitivity without implying predictive validity. |
| Evidence clearing | 10 | Turn source, locator, conflict, inference, and version questions into fail-closed preflight procedures. |

Every guide has a direct answer, at least four inputs, five ordered steps, three outputs, three refusal conditions, three limitations, named public sources or projections, five bounded questions, canonical metadata, TechArticle/FAQ structured data, and a SHA-256 provenance digest.

## Discovery and internal structure

The 100 paths are added to the sitemap and `llms.txt` from the same deterministic registry. Curated links originate from the developer, religion, astrology, and epistemic-system hubs. Each dynamic family has a finite `generateStaticParams` set and rejects unknown slugs.

## Publication boundary

These are editorial and operational guide pages, not new canonical scientific records. Existing inspected-source projections may be linked, but the new guide does not inherit or widen their evidence. Book sections are labelled conceptual lenses. Astrology calculations are not presented as validation. Evidence Preflight remains structural triage rather than a verified Evidence Dossier.

No route in this batch is claimed live, indexed, observed, commercially validated, or deployed. The previous local build produced 993 static pages; adding this prepared batch gives a planning projection of 1,093, not a measured build result. On that planning basis, 407 additional pages remain before the 1,500-page build threshold. The exact count must be established only during the later operator-authorized build.

## Verification allowed under the embargo

- Deterministic regeneration and digest recomputation
- Focused Node tests
- Type checking
- Linting
- Diff and boundary inspection

Disallowed before the threshold and explicit approval:

- `next build` or `npm run build`
- Vercel Preview or Production build
- Preview or Production deployment
- Any public-route or canonical-release claim based on prepared files alone

