# Frontier source-alignment audit

Audit `maha-frontier-source-alignment/1.0` · input date `2026-08-26` · digest `sha256:e1d95e9b8d43f11ecd8377719b411855ba83a14109f9f09cb7711d5f483b506c`

This file is generated by `scripts/generate-frontier-alignment-report.ts`. Do not edit it by hand.

A source resolving is a structural fact. It does not show that the source is about the record subject. This audit records what was actually checked for each of the 240 frontier records, and keeps five things separate rather than collapsing them into one verified flag: metadata verified, source inspected, subject aligned, claim supported, independently reproduced.

**Nothing here is independent expert review.** It is internal editorial verification. No entry claims scientific truth, commercial readiness, reproduction, or external endorsement, and `independentlyReproduced` is false everywhere.

## Coverage

| Measure | Count |
| --- | --- |
| Frontier records audited | 240 |
| Source inspected | 12 |
| Metadata verified but not inspected | 138 |
| Mappings corrected | 2 |
| Alignment-clear | 11 |
| Blocked from substantial-page generation | 229 |

## Verdicts

| Verdict | Count |
| --- | --- |
| supported | 11 |
| partially-supported | 1 |
| mismatched | 3 |
| insufficient-evidence | 220 |
| inaccessible-source | 5 |

## Assignment origin

| Origin | Count |
| --- | --- |
| explicit-override | 2 |
| positional-legacy | 229 |
| independently-curated | 9 |

**229 mappings remain positional.** Those were never chosen for the concept they are attached to, and none is treated as supported.

## Blockers

| Blocker | Records |
| --- | --- |
| source-alignment-insufficient-evidence | 220 |
| source-assignment-positional-legacy | 229 |
| source-inaccessible | 5 |
| source-metadata-unverified | 90 |
| source-not-inspected | 228 |
| source-subject-mismatched | 3 |
| source-subject-partially-supported | 1 |

## By domain

| Domain | Records | supported | partially-supported | mismatched | insufficient-evidence | inaccessible-source | Clear |
| --- | --- | --- | --- | --- | --- | --- | --- |
| advanced-materials | 30 | 1 | 0 | 3 | 26 | 0 | 1 |
| agentic-systems-mcp | 30 | 5 | 0 | 0 | 25 | 0 | 5 |
| biomolecular-engineering | 30 | 0 | 0 | 0 | 30 | 0 | 0 |
| critical-supply-chains | 30 | 0 | 0 | 0 | 25 | 5 | 0 |
| fusion-plasma-systems | 30 | 4 | 1 | 0 | 25 | 0 | 4 |
| longevity-metabolism | 30 | 0 | 0 | 0 | 30 | 0 | 0 |
| mechanistic-interpretability | 30 | 0 | 0 | 0 | 30 | 0 | 0 |
| neurotechnology-bci | 30 | 1 | 0 | 0 | 29 | 0 | 1 |

## Confirmed mappings

| Record | Source | Inspected at | Origin |
| --- | --- | --- | --- |
| `advanced-materials-hexagonal-boron-nitride-dielectrics` | Boron nitride substrates for high-quality graphene electronics | Abstract | explicit-override |
| `agentic-systems-mcp-mcp-capability-negotiation` | Model Context Protocol specification: Tools | Capabilities section | independently-curated |
| `agentic-systems-mcp-mcp-client-server-roles` | Model Context Protocol specification: Tools | Protocol Messages and Message Flow sections | independently-curated |
| `agentic-systems-mcp-mcp-tool-discovery` | Model Context Protocol specification: Tools | Protocol Messages, Listing Tools section | independently-curated |
| `agentic-systems-mcp-mcp-tool-input-schemas` | Model Context Protocol specification: Tools | Data Types, Tool definition section | independently-curated |
| `agentic-systems-mcp-mcp-tool-result-contracts` | Model Context Protocol specification: Tools | Data Types, Tool Result and Structured Content sections | independently-curated |
| `fusion-plasma-systems-central-solenoid-inductive-drive` | Magnets | Machine / Magnets, central solenoid section | independently-curated |
| `fusion-plasma-systems-magnetic-confinement` | Magnets | Machine / Magnets, toroidal field section | independently-curated |
| `fusion-plasma-systems-poloidal-field-coils` | Magnets | Machine / Magnets, poloidal field section | independently-curated |
| `fusion-plasma-systems-toroidal-field-coils` | Magnets | Machine / Magnets, toroidal field section | independently-curated |
| `neurotechnology-bci-spike-sorting-boundaries` | Quality Metrics to Accompany Spike Sorting of Extracellular Signals | Quality metrics and Summary matrices sections | explicit-override |

## Corrected mappings

| Record | Prior source | Current source | Why it was replaced |
| --- | --- | --- | --- |
| `advanced-materials-hexagonal-boron-nitride-dielectrics` | `source-advanced-materials-graphene` — Electric Field Effect in Atomically Thin Carbon Films | `source-advanced-materials-hbn-substrates` | Positional block source. Studied atomically thin carbon and never mentioned boron nitride. Replaced after inspection. |
| `neurotechnology-bci-spike-sorting-boundaries` | `source-neurotechnology-bci-neuropixels` — Fully integrated silicon probes for high-density recording of neural activity | `source-neurotechnology-bci-spike-sorting-quality-metrics` | Positional block source. Reported instrumentation rather than the sorting step. Replaced after inspection. |

## Mismatches

| Record | Bound source | Finding | Remediation |
| --- | --- | --- | --- |
| `advanced-materials-graphene-hbn-heterostructures` | Electric Field Effect in Atomically Thin Carbon Films | The bound source is registered as "Electric Field Effect in Atomically Thin Carbon Films", Science 2004. It concerns transport in atomically thin carbon and does not involve boron nitride, so it cannot be about graphene-hBN heterostructures. | Bind to a source that reports graphene on hexagonal boron nitride. Dean et al. (2010) is already in this corpus and is a candidate, but was not read for this record. |
| `advanced-materials-moire-superlattices` | Electric Field Effect in Atomically Thin Carbon Films | The bound source is the 2004 atomically thin carbon paper. Moire superlattice physics in these systems is later work, so a 2004 transport paper cannot establish the record subject. | Bind to a source that reports moire superlattice formation or measurement. |
| `advanced-materials-twist-angle-control` | Electric Field Effect in Atomically Thin Carbon Films | The bound source is the 2004 atomically thin carbon paper, which involves no twisted stack and no angle control. The subject postdates it. | Bind to a source that reports twist-angle control in assembled stacks. |

These verdicts rest on registry metadata rather than inspection. That asymmetry is deliberate: a mismatch can only block a page, never pass one, so it is safe to establish from a registered title and date. Support is never inferred the same way.

## Inaccessible sources

| Record | Source contract | Finding |
| --- | --- | --- |
| `critical-supply-chains-euv-photoresist-precursors` | `source-critical-supply-chains-pp1802` | The declared source URL was requested during this audit and returned HTTP 403, so the content could not be inspected. An inaccessible source is never treated as content-confirmed. |
| `critical-supply-chains-high-purity-quartz-deposits` | `source-critical-supply-chains-pp1802` | The declared source URL was requested during this audit and returned HTTP 403, so the content could not be inspected. An inaccessible source is never treated as content-confirmed. |
| `critical-supply-chains-photoacid-generator-supply` | `source-critical-supply-chains-pp1802` | The declared source URL was requested during this audit and returned HTTP 403, so the content could not be inspected. An inaccessible source is never treated as content-confirmed. |
| `critical-supply-chains-quartz-crucible-manufacturing` | `source-critical-supply-chains-pp1802` | The declared source URL was requested during this audit and returned HTTP 403, so the content could not be inspected. An inaccessible source is never treated as content-confirmed. |
| `critical-supply-chains-semiconductor-grade-polysilicon` | `source-critical-supply-chains-pp1802` | The declared source URL was requested during this audit and returned HTTP 403, so the content could not be inspected. An inaccessible source is never treated as content-confirmed. |

## Unresolved work queue

**229 records remain.** Each needs its bound source read against its own subject, after which the mapping is either confirmed as curated or replaced by an explicit override. The queue below is ordered by record id and is exact.

| Record | Source contract | Verdict | Remediation |
| --- | --- | --- | --- |
| `advanced-materials-color-centers-in-diamond` | `source-advanced-materials-wide-bandgap` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `advanced-materials-contact-resistance-in-2d-devices` | `source-advanced-materials-vdw` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `advanced-materials-correlated-insulating-states` | `source-advanced-materials-magic-angle` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `advanced-materials-cvd-graphene-grain-boundaries` | `source-advanced-materials-vdw` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `advanced-materials-diamond-thermal-conductivity` | `source-advanced-materials-wide-bandgap` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `advanced-materials-diamond-wafer-substrates` | `source-advanced-materials-wide-bandgap` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `advanced-materials-dielectric-screening` | `source-advanced-materials-vdw` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `advanced-materials-direct-gap-mos2` | `source-advanced-materials-magic-angle` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `advanced-materials-dry-transfer-contamination` | `source-advanced-materials-topological` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `advanced-materials-encapsulation-boundaries` | `source-advanced-materials-topological` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `advanced-materials-gallium-nitride-epitaxy` | `source-advanced-materials-wide-bandgap` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `advanced-materials-graphene-hbn-heterostructures` | `source-advanced-materials-graphene` | mismatched | Bind to a source that reports graphene on hexagonal boron nitride. Dean et al. (2010) is already in this corpus and is a candidate, but was not read for this record. |
| `advanced-materials-graphene-monolayers` | `source-advanced-materials-graphene` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `advanced-materials-interface-bubbles-and-strain` | `source-advanced-materials-topological` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `advanced-materials-interlayer-excitons` | `source-advanced-materials-tmd` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `advanced-materials-magic-angle-superconductivity` | `source-advanced-materials-magic-angle` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `advanced-materials-materials-metrology-transfer` | `source-advanced-materials-vdw` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `advanced-materials-moire-superlattices` | `source-advanced-materials-graphene` | mismatched | Bind to a source that reports moire superlattice formation or measurement. |
| `advanced-materials-quantum-anomalous-hall-state` | `source-advanced-materials-tmd` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `advanced-materials-sic-wide-bandgap-substrates` | `source-advanced-materials-wide-bandgap` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `advanced-materials-spin-momentum-locking` | `source-advanced-materials-tmd` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `advanced-materials-tmd-heterobilayers` | `source-advanced-materials-tmd` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `advanced-materials-tmd-monolayers` | `source-advanced-materials-magic-angle` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `advanced-materials-topological-insulator-surface-states` | `source-advanced-materials-tmd` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `advanced-materials-twist-angle-control` | `source-advanced-materials-graphene` | mismatched | Bind to a source that reports twist-angle control in assembled stacks. |
| `advanced-materials-two-dimensional-magnetism` | `source-advanced-materials-topological` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `advanced-materials-valley-polarized-excitons` | `source-advanced-materials-magic-angle` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `advanced-materials-van-der-waals-assembly` | `source-advanced-materials-topological` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `advanced-materials-wafer-scale-2d-growth` | `source-advanced-materials-vdw` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `agentic-systems-mcp-agent-memory-boundaries` | `source-agentic-systems-mcp-autogen` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `agentic-systems-mcp-agent-plan-execution-separation` | `source-agentic-systems-mcp-react` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `agentic-systems-mcp-agentic-evaluation-protocols` | `source-agentic-systems-mcp-autogen` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `agentic-systems-mcp-context-window-position-effects` | `source-agentic-systems-mcp-lost-middle` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `agentic-systems-mcp-context-window-token-degradation` | `source-agentic-systems-mcp-lost-middle` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `agentic-systems-mcp-distributed-agent-consensus` | `source-agentic-systems-mcp-react` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `agentic-systems-mcp-game-theoretic-incentive-misalignment` | `source-agentic-systems-mcp-autogen` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `agentic-systems-mcp-human-approval-boundaries` | `source-agentic-systems-mcp-lost-middle` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `agentic-systems-mcp-idempotent-tool-calls` | `source-agentic-systems-mcp-nist-genai` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `agentic-systems-mcp-least-authority-tokens` | `source-agentic-systems-mcp-nist-genai` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `agentic-systems-mcp-mcp-prompt-templates` | `source-agentic-systems-mcp-mcp-core` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `agentic-systems-mcp-mcp-resource-discovery` | `source-agentic-systems-mcp-mcp-core` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `agentic-systems-mcp-mcp-session-lifecycle` | `source-agentic-systems-mcp-mcp-core` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `agentic-systems-mcp-multi-agent-coordination-protocols` | `source-agentic-systems-mcp-react` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `agentic-systems-mcp-multi-agent-deadlock` | `source-agentic-systems-mcp-react` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `agentic-systems-mcp-multi-agent-role-assignment` | `source-agentic-systems-mcp-react` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `agentic-systems-mcp-prompt-injection-through-tools` | `source-agentic-systems-mcp-autogen` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `agentic-systems-mcp-retrieval-context-selection` | `source-agentic-systems-mcp-lost-middle` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `agentic-systems-mcp-sandboxed-tool-execution` | `source-agentic-systems-mcp-nist-genai` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `agentic-systems-mcp-tool-allowlisting` | `source-agentic-systems-mcp-mcp-core` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `agentic-systems-mcp-tool-call-traces` | `source-agentic-systems-mcp-nist-genai` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `agentic-systems-mcp-tool-deny-by-default` | `source-agentic-systems-mcp-mcp-core` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `agentic-systems-mcp-tool-output-trust-boundaries` | `source-agentic-systems-mcp-autogen` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `agentic-systems-mcp-tool-result-context-injection` | `source-agentic-systems-mcp-lost-middle` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `agentic-systems-mcp-tool-timeout-budgets` | `source-agentic-systems-mcp-nist-genai` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-cell-free-reaction-yield` | `source-biomolecular-engineering-cell-free` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-cell-free-transcription-translation` | `source-biomolecular-engineering-cell-free` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-compartmentalized-cell-free-systems` | `source-biomolecular-engineering-toehold` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-crude-extract-cell-free-systems` | `source-biomolecular-engineering-cell-free` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-de-novo-binder-design` | `source-biomolecular-engineering-rfdiffusion` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-design-to-assay-provenance` | `source-biomolecular-engineering-proteinmpnn` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-directed-enzyme-evolution` | `source-biomolecular-engineering-pace` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-droplet-microfluidic-screening` | `source-biomolecular-engineering-toehold` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-energy-regeneration-in-cell-free-systems` | `source-biomolecular-engineering-cell-free` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-enzyme-cascade-engineering` | `source-biomolecular-engineering-toehold` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-experimental-fold-validation` | `source-biomolecular-engineering-proteinmpnn` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-fitness-landscape-accessibility` | `source-biomolecular-engineering-pace` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-ligand-binding-aptamer-domains` | `source-biomolecular-engineering-riboregulators` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-metabolic-pathway-prototyping` | `source-biomolecular-engineering-toehold` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-motif-scaffolding` | `source-biomolecular-engineering-rfdiffusion` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-mutation-library-generation` | `source-biomolecular-engineering-pace` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-off-target-binding-characterization` | `source-biomolecular-engineering-proteinmpnn` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-phage-assisted-continuous-evolution` | `source-biomolecular-engineering-pace` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-protein-backbone-diffusion` | `source-biomolecular-engineering-rfdiffusion` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-protein-design-success-rate` | `source-biomolecular-engineering-proteinmpnn` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-purified-component-expression-systems` | `source-biomolecular-engineering-cell-free` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-riboswitch-expression-platforms` | `source-biomolecular-engineering-riboregulators` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-rna-strand-displacement` | `source-biomolecular-engineering-riboregulators` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-selection-pressure-coupling` | `source-biomolecular-engineering-pace` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-sequence-design-with-proteinmpnn` | `source-biomolecular-engineering-proteinmpnn` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-structure-prediction-filtering` | `source-biomolecular-engineering-rfdiffusion` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-synthetic-riboswitches` | `source-biomolecular-engineering-riboregulators` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-toehold-switches` | `source-biomolecular-engineering-riboregulators` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-translational-control-circuits` | `source-biomolecular-engineering-toehold` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `biomolecular-engineering-unconditional-protein-generation` | `source-biomolecular-engineering-rfdiffusion` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `critical-supply-chains-antimony-smelting-concentration` | `source-critical-supply-chains-mcs-specialty` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `critical-supply-chains-cobalt-refining-concentration` | `source-critical-supply-chains-mcs-industrial` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `critical-supply-chains-critical-mineral-import-reliance` | `source-critical-supply-chains-supply-analysis` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `critical-supply-chains-dysprosium-ore-to-oxide` | `source-critical-supply-chains-mcs-rare-earths` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `critical-supply-chains-euv-photoresist-precursors` | `source-critical-supply-chains-pp1802` | inaccessible-source | Obtain the document through a retrievable route, then inspect it and record the exact location. |
| `critical-supply-chains-export-control-exposure` | `source-critical-supply-chains-supply-analysis` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `critical-supply-chains-fluorinated-resist-components` | `source-critical-supply-chains-mcs-gallium-germanium` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `critical-supply-chains-gallium-bauxite-byproduct-flow` | `source-critical-supply-chains-mcs-gallium-germanium` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `critical-supply-chains-gallium-zinc-processing-byproduct` | `source-critical-supply-chains-mcs-gallium-germanium` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `critical-supply-chains-germanium-coal-ash-recovery` | `source-critical-supply-chains-mcs-gallium-germanium` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `critical-supply-chains-germanium-zinc-refining-flow` | `source-critical-supply-chains-mcs-gallium-germanium` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `critical-supply-chains-graphite-anode-processing` | `source-critical-supply-chains-mcs-industrial` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `critical-supply-chains-hafnium-zirconium-separation` | `source-critical-supply-chains-mcs-specialty` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `critical-supply-chains-heavy-rare-earth-diffusion` | `source-critical-supply-chains-mcs-rare-earths` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `critical-supply-chains-helium-liquefaction-logistics` | `source-critical-supply-chains-mcs-specialty` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `critical-supply-chains-high-purity-quartz-deposits` | `source-critical-supply-chains-pp1802` | inaccessible-source | Obtain the document through a retrievable route, then inspect it and record the exact location. |
| `critical-supply-chains-indium-zinc-byproduct-flow` | `source-critical-supply-chains-mcs-industrial` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `critical-supply-chains-magnet-recycling` | `source-critical-supply-chains-mcs-industrial` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `critical-supply-chains-material-substitution-boundaries` | `source-critical-supply-chains-supply-analysis` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `critical-supply-chains-nd-fe-b-magnet-alloying` | `source-critical-supply-chains-mcs-rare-earths` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `critical-supply-chains-neodymium-praseodymium-separation` | `source-critical-supply-chains-mcs-rare-earths` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `critical-supply-chains-niobium-ferroniobium-production` | `source-critical-supply-chains-mcs-specialty` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `critical-supply-chains-photoacid-generator-supply` | `source-critical-supply-chains-pp1802` | inaccessible-source | Obtain the document through a retrievable route, then inspect it and record the exact location. |
| `critical-supply-chains-quartz-crucible-manufacturing` | `source-critical-supply-chains-pp1802` | inaccessible-source | Obtain the document through a retrievable route, then inspect it and record the exact location. |
| `critical-supply-chains-rare-earth-solvent-extraction` | `source-critical-supply-chains-mcs-rare-earths` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `critical-supply-chains-semiconductor-grade-polysilicon` | `source-critical-supply-chains-pp1802` | inaccessible-source | Obtain the document through a retrievable route, then inspect it and record the exact location. |
| `critical-supply-chains-single-country-processing-concentration` | `source-critical-supply-chains-supply-analysis` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `critical-supply-chains-supply-chain-data-uncertainty` | `source-critical-supply-chains-supply-analysis` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `critical-supply-chains-tantalum-concentrate-traceability` | `source-critical-supply-chains-mcs-specialty` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `critical-supply-chains-tungsten-concentrate-processing` | `source-critical-supply-chains-mcs-industrial` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `fusion-plasma-systems-breeding-blanket-test-modules` | `source-fusion-plasma-systems-iter-support` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `fusion-plasma-systems-cable-in-conduit-conductors` | `source-fusion-plasma-systems-stellarator-review` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `fusion-plasma-systems-cryogenic-magnet-cooling` | `source-fusion-plasma-systems-iter-support` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `fusion-plasma-systems-disruption-mitigation` | `source-fusion-plasma-systems-iter-disruption` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `fusion-plasma-systems-divertor-heat-exhaust` | `source-fusion-plasma-systems-iter-divertor` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `fusion-plasma-systems-edge-localized-modes` | `source-fusion-plasma-systems-iter-divertor` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `fusion-plasma-systems-electron-cyclotron-heating` | `source-fusion-plasma-systems-iter-disruption` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `fusion-plasma-systems-inertial-confinement-target-chamber` | `source-fusion-plasma-systems-nif-ignition` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `fusion-plasma-systems-laser-target-coupling` | `source-fusion-plasma-systems-nif-ignition` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `fusion-plasma-systems-magnetic-mirror-confinement` | `source-fusion-plasma-systems-nif-ignition` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `fusion-plasma-systems-neutral-beam-injection` | `source-fusion-plasma-systems-iter-disruption` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `fusion-plasma-systems-neutron-material-damage` | `source-fusion-plasma-systems-nif-ignition` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `fusion-plasma-systems-plasma-diagnostics` | `source-fusion-plasma-systems-iter-support` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `fusion-plasma-systems-plasma-facing-components` | `source-fusion-plasma-systems-iter-divertor` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `fusion-plasma-systems-plasma-heating-and-current-drive` | `source-fusion-plasma-systems-iter-disruption` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `fusion-plasma-systems-plasma-position-and-shape-control` | `source-fusion-plasma-systems-iter-divertor` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `fusion-plasma-systems-rebco-high-field-magnets` | `source-fusion-plasma-systems-stellarator-review` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `fusion-plasma-systems-remote-handling-maintenance` | `source-fusion-plasma-systems-nif-ignition` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `fusion-plasma-systems-resonant-magnetic-perturbations` | `source-fusion-plasma-systems-iter-divertor` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `fusion-plasma-systems-shattered-pellet-injection` | `source-fusion-plasma-systems-iter-disruption` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `fusion-plasma-systems-stellarator-field-optimization` | `source-fusion-plasma-systems-stellarator-review` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `fusion-plasma-systems-stellarator-magnetic-coils` | `source-fusion-plasma-systems-stellarator-review` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `fusion-plasma-systems-superconducting-quench-protection` | `source-fusion-plasma-systems-stellarator-review` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `fusion-plasma-systems-tokamak-plasma-equilibrium` | `source-fusion-plasma-systems-iter-magnets` | partially-supported | Bind to a plasma-physics source that treats equilibrium directly, or narrow the record to the coil systems that act on it. |
| `fusion-plasma-systems-tritium-fuel-cycle` | `source-fusion-plasma-systems-iter-support` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `fusion-plasma-systems-vacuum-vessel-boundary` | `source-fusion-plasma-systems-iter-support` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-ampk-energy-sensing` | `source-longevity-metabolism-bioenergetics` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-apoptosis-in-senescent-cells` | `source-longevity-metabolism-senolytics` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-atp-linked-respiration` | `source-longevity-metabolism-bioenergetics` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-autophagic-flux` | `source-longevity-metabolism-autophagy-guidelines` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-autophagosome-abundance` | `source-longevity-metabolism-autophagy-guidelines` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-cd38-nad-consumption` | `source-longevity-metabolism-hallmarks` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-cellular-senescence-markers` | `source-longevity-metabolism-senolytics` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-insulin-mtor-signaling` | `source-longevity-metabolism-bioenergetics` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-intervention-biomarker-boundaries` | `source-longevity-metabolism-hallmarks` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-lc3-turnover-assays` | `source-longevity-metabolism-autophagy-guidelines` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-lifespan-versus-healthspan-endpoints` | `source-longevity-metabolism-hallmarks` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-lysosomal-degradation-blockade` | `source-longevity-metabolism-autophagy-guidelines` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-metabolic-flux-tracing` | `source-longevity-metabolism-bioenergetics` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-mitochondrial-membrane-potential` | `source-longevity-metabolism-mitophagy` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-mitochondrial-uncoupling` | `source-longevity-metabolism-mitophagy` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-mitophagy-flux` | `source-longevity-metabolism-mitophagy` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-nad-consumption-by-parps` | `source-longevity-metabolism-nad` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-nad-salvage-pathway` | `source-longevity-metabolism-nad` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-nampt-rate-limiting-step` | `source-longevity-metabolism-nad` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-nmn-and-nr-precursors` | `source-longevity-metabolism-nad` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-nmnat-compartmentalization` | `source-longevity-metabolism-nad` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-oxygen-consumption-rate` | `source-longevity-metabolism-bioenergetics` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-p62-sqstm1-turnover` | `source-longevity-metabolism-autophagy-guidelines` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-pink1-parkin-pathway` | `source-longevity-metabolism-mitophagy` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-proton-leak-respiration` | `source-longevity-metabolism-mitophagy` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-senescence-associated-secretory-phenotype` | `source-longevity-metabolism-senolytics` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-senescent-cell-clearance` | `source-longevity-metabolism-senolytics` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-senolytic-selectivity` | `source-longevity-metabolism-senolytics` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-sirtuin-nad-dependence` | `source-longevity-metabolism-hallmarks` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `longevity-metabolism-translation-to-human-outcomes` | `source-longevity-metabolism-hallmarks` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-activation-patching` | `source-mechanistic-interpretability-causal-scrubbing` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-attention-pattern-evidence` | `source-mechanistic-interpretability-induction` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-automated-feature-interpretation` | `source-mechanistic-interpretability-feature-visualization` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-benchmark-task-transfer` | `source-mechanistic-interpretability-circuits` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-causal-feature-intervention` | `source-mechanistic-interpretability-feature-visualization` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-causal-scrubbing` | `source-mechanistic-interpretability-causal-scrubbing` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-circuit-completeness` | `source-mechanistic-interpretability-circuits` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-circuit-faithfulness` | `source-mechanistic-interpretability-circuits` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-cross-layer-transcoders` | `source-mechanistic-interpretability-feature-visualization` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-dead-features` | `source-mechanistic-interpretability-feature-visualization` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-feature-absorption` | `source-mechanistic-interpretability-sae` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-feature-activation-maximization` | `source-mechanistic-interpretability-feature-visualization` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-feature-splitting` | `source-mechanistic-interpretability-sae` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-in-context-learning-circuits` | `source-mechanistic-interpretability-induction` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-induction-head-circuits` | `source-mechanistic-interpretability-induction` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-interchange-interventions` | `source-mechanistic-interpretability-causal-scrubbing` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-interpretability-claim-boundaries` | `source-mechanistic-interpretability-circuits` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-io-identification-circuit` | `source-mechanistic-interpretability-induction` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-mechanistic-anomaly-detection` | `source-mechanistic-interpretability-circuits` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-model-component-ablation` | `source-mechanistic-interpretability-causal-scrubbing` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-neural-feature-superposition` | `source-mechanistic-interpretability-superposition` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-path-patching` | `source-mechanistic-interpretability-causal-scrubbing` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-polysemantic-neurons` | `source-mechanistic-interpretability-superposition` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-previous-token-heads` | `source-mechanistic-interpretability-induction` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-representation-probing-boundary` | `source-mechanistic-interpretability-superposition` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-sae-encoder-decoder` | `source-mechanistic-interpretability-sae` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-sae-sparsity-fidelity-tradeoff` | `source-mechanistic-interpretability-sae` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-sparse-autoencoder-dictionaries` | `source-mechanistic-interpretability-sae` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-superposition-geometry` | `source-mechanistic-interpretability-superposition` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `mechanistic-interpretability-toy-models-of-superposition` | `source-mechanistic-interpretability-superposition` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-adaptive-stimulation-policies` | `source-neurotechnology-bci-closed-loop` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-bci-calibration-drift` | `source-neurotechnology-bci-intracortical-bci` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-channelrhodopsin-photocurrent-kinetics` | `source-neurotechnology-bci-channelrhodopsin` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-chronic-signal-stability` | `source-neurotechnology-bci-foreign-body` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-clinical-translation-boundaries` | `source-neurotechnology-bci-foreign-body` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-closed-loop-neural-decoding` | `source-neurotechnology-bci-closed-loop` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-cortical-surface-bci` | `source-neurotechnology-bci-intracortical-bci` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-decoder-update-latency` | `source-neurotechnology-bci-closed-loop` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-electrocorticography-spatial-resolution` | `source-neurotechnology-bci-micro-ecog` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-electrode-tissue-interface` | `source-neurotechnology-bci-micro-ecog` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-extracellular-spike-recording` | `source-neurotechnology-bci-neuropixels` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-flexible-conformal-electrode-arrays` | `source-neurotechnology-bci-micro-ecog` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-foreign-body-response` | `source-neurotechnology-bci-foreign-body` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-impedance-and-noise` | `source-neurotechnology-bci-micro-ecog` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-intracortical-bci` | `source-neurotechnology-bci-intracortical-bci` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-light-delivery-tissue-heating` | `source-neurotechnology-bci-channelrhodopsin` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-micro-ecog-arrays` | `source-neurotechnology-bci-micro-ecog` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-motor-intention-decoding` | `source-neurotechnology-bci-intracortical-bci` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-neural-data-compression` | `source-neurotechnology-bci-foreign-body` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-neural-feature-extraction` | `source-neurotechnology-bci-closed-loop` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-neuropixels-channel-selection` | `source-neurotechnology-bci-neuropixels` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-neuropixels-cmos-probe` | `source-neurotechnology-bci-neuropixels` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-neuropixels-recording-sites` | `source-neurotechnology-bci-neuropixels` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-opsin-spectral-sensitivity` | `source-neurotechnology-bci-channelrhodopsin` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-optogenetic-channelrhodopsin` | `source-neurotechnology-bci-channelrhodopsin` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-peripheral-nerve-interface` | `source-neurotechnology-bci-intracortical-bci` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-sensing-to-stimulation-loop` | `source-neurotechnology-bci-closed-loop` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-stimulation-artifact-rejection` | `source-neurotechnology-bci-channelrhodopsin` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |
| `neurotechnology-bci-wireless-neural-telemetry` | `source-neurotechnology-bci-foreign-body` | insufficient-evidence | Inspect the bound source against this record subject, then either confirm the mapping as curated or replace it with an explicit override. |

