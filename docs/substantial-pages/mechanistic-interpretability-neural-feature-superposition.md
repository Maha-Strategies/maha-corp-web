# Neural feature superposition — substantial-page packet

This file is generated by `scripts/generate-substantial-page-pilots.ts`. Do not edit it by hand.

**Internal and noncanonical.** These packets are not routed, not in sitemap.xml, not in llms.txt, and not promoted. Compiling a page does not publish a record.

Record `urn:maha:record:mechanistic-interpretability-neural-feature-superposition` · concept · domain `mechanistic-interpretability` · review state `draft`

Contract digest `sha256:a6b6162118b689c314bef3d23e8002416aec21b93a59150428c67cf55920b43a`

Record revision `sha256:05a72058cb2682388462fcf950bdd95334b41c6ad0869a5e0f457d3c505eacea`

Gate: **BLOCK**

- `source-alignment-insufficient-evidence`
- `source-assignment-positional-legacy`
- `source-metadata-unverified`
- `source-not-inspected`

## Why this record was chosen

The most connected record in the domain with six declared edges, a complete locator and rights basis. Its source addresses the subject directly, and it is the endpoint a Q-BR bridge tried and failed to reach, which makes its boundaries worth rendering carefully.

## Source alignment

**insufficient-evidence** — Toy Models of Superposition develops the superposition account directly, and the locator names the definitions, toy models, geometry and sparsity sections.

## Search intent

| Field | Value |
| --- | --- |
| Primary query | neural feature superposition |
| Reader question | What is superposition in a neural network, and what did the toy-model work actually show? |
| Audience | Readers of interpretability research who need toy-model results kept separate from claims about production models. |
| Reader outcome | The reader can define superposition, state the sparsity conditions under which it was demonstrated, and avoid transferring toy-model geometry to production systems. |
| Title | Neural feature superposition: what toy models showed (52 chars) |
| Description | Superposition as an account of networks representing more features than dimensions, the sparsity conditions the toy models assumed, and why production models differ. (165 chars) |

Supporting questions:

- What does it mean for a network to represent more features than dimensions?
- What role does sparsity play in superposition?
- Do toy-model results describe production language models?
- How does superposition relate to polysemantic neurons?

Query variants:

- what is superposition neural network
- toy models of superposition explained
- features than dimensions interpretability

_This contract describes what the page says and what it declines to say. It does not guarantee rankings, impressions, traffic, or any commercial outcome._

## Rendered content preview

### Direct answer

Superposition is the proposal that a network can represent more distinct features than it has dimensions by assigning them overlapping, non-orthogonal directions, tolerating interference in exchange for capacity. The cited work develops toy models exhibiting this under specified sparsity conditions. It does not establish that every feature in a production model has the same geometry or semantics.

Bound to `urn:maha:claim:mechanistic-interpretability-neural-feature-superposition` via `source-mechanistic-interpretability-superposition`.

### What it is

If a layer has a fixed number of dimensions, the naive expectation is that it can cleanly represent at most that many features. Superposition is the account under which a network instead packs more features into the space by giving them directions that are not orthogonal, accepting some interference between them as the price of extra capacity.

The record is registered as a concept and is bounded to the definitions, toy models, geometry, sparsity and feature-interference experiments named in its locator. It is a model of how representation might be organised, not a measurement taken from a deployed system.

Bound to `urn:maha:claim:mechanistic-interpretability-neural-feature-superposition` via `source-mechanistic-interpretability-superposition`.

### How the cited evidence works

The evidence is Toy Models of Superposition, published on the Transformer Circuits Thread on 14 September 2022. The work constructs small models where the number of features, the dimensionality and the sparsity of feature occurrence are all controlled, then examines the geometry the models adopt.

The controlled setting is what gives the result its force and also what bounds it. Sparsity is an assumption of the construction rather than a measured property of a production network, so the demonstration is that superposition occurs under stated conditions, recorded here at single-study maturity as a theoretical model.

Bound to `urn:maha:claim:mechanistic-interpretability-neural-feature-superposition` via `source-mechanistic-interpretability-superposition`.

### What remains unresolved

Whether production models exhibit the same geometry is unresolved. A toy-model mechanism does not establish that features in a large deployed network share that structure or that any particular direction found there carries the semantics an interpreter assigns to it.

The relationship between superposition and downstream interpretability methods is also unresolved on this record. Techniques that assume a sparse overcomplete basis are motivated by this account but are not validated by it, and treating the motivation as evidence for the method is precisely the transfer this record forbids.

Bound to `urn:maha:claim:mechanistic-interpretability-neural-feature-superposition` via `source-mechanistic-interpretability-superposition`.

## Source and claim alignment

| Claim | Declared sources | Source title | Exact locator | Rights |
| --- | --- | --- | --- | --- |
| `urn:maha:claim:mechanistic-interpretability-neural-feature-superposition` | `source-mechanistic-interpretability-superposition` | Toy Models of Superposition | Definitions, toy models, geometry, sparsity, and feature-interference experiments. | citation-with-paraphrase |

## Comparison applicability

**not-applicable** — A comparison table for neural feature superposition would need claims on both sides of an axis. The record cites one work and carries one claim, and the natural contrast, production-model geometry, is precisely what the source does not measure. Presenting one cited source as a two-sided comparison would manufacture a contrast the evidence does not contain.

## Calculation applicability

**not-applicable** — Feature counts, dimensionality and sparsity are parameters chosen inside the cited construction rather than quantities this record fixes, so an expression here would imply a general capacity formula the source does not assert. Adding an expression here would create unsupported precision: it would imply the record fixes quantities that its cited locator does not fix, and a reader could reproduce a number that no source stands behind.

## Limitation inventory

| # | Basis | Index | Statement |
| --- | --- | --- | --- |
| 1 | record-boundary | 0 | Neural feature superposition does not by itself establish system-level performance, safety, manufacturability, scalability, economic advantage, clinical benefit, or deployment readiness. |
| 2 | record-boundary | 1 | A source-bounded mechanism, method, or measurement record does not establish manufacturing yield, economic advantage, safety, clinical benefit, or commercial readiness unless those outcomes are measured in a separately scoped record. |
| 3 | prohibited-inference | 0 | Do not use this neural feature superposition record to claim that the surrounding technology is proven, safe, scalable, commercially available, or strategically superior. |
| 4 | prohibited-inference | 1 | Do not transfer a reported result across hardware, organisms, protocols, datasets, operating conditions, or outcome definitions without a declared comparison contract. |

## Related-record inventory

| Record | Relation | Tier | Rationale |
| --- | --- | --- | --- |
| `urn:maha:record:mechanistic-interpretability-activation-patching` | boundary | bridge-edge | Declared strategic-dependency edge into this record, so it is positioned earlier in the same bounded sequence. |
| `urn:maha:record:mechanistic-interpretability-circuit-completeness` | boundary | bridge-edge | Declared strategic-dependency edge into this record, so it is positioned earlier in the same bounded sequence. |
| `urn:maha:record:mechanistic-interpretability-dead-features` | boundary | bridge-edge | Declared strategic-dependency edge into this record, so it is positioned earlier in the same bounded sequence. |
| `urn:maha:record:mechanistic-interpretability-induction-head-circuits` | boundary | bridge-edge | Declared strategic-dependency edge into this record, so it is positioned earlier in the same bounded sequence. |
| `urn:maha:record:mechanistic-interpretability-polysemantic-neurons` | prerequisite | bridge-edge | Declared mechanistic-dependency edge into this record, so it is positioned earlier in the same bounded sequence. |
| `urn:maha:record:mechanistic-interpretability-sparse-autoencoder-dictionaries` | boundary | bridge-edge | Declared strategic-dependency edge into this record, so it is positioned earlier in the same bounded sequence. |

## Original contribution

The contribution is keeping the toy-model boundary attached to the concept itself, so that the sparsity assumption travels with the idea rather than being dropped when superposition is invoked to justify a downstream interpretability method.

