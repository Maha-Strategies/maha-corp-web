# Lake build output and toolchain caches. Never committed: build artifacts
# cannot be shown to correspond to the source they claim to come from, and the
# paths inside them are machine-specific.
.lake/
build/
lakefile.olean
