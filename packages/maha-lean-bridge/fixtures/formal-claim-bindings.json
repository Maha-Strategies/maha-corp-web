{
  "schemaVersion": "maha-formal-claim-binding/0.1",
  "bindings": [
    {
      "bindingId": "bnd_interval_add_valid",
      "revision": 1,
      "dossierId": "dos_internal_interval_tolerance_fixture",
      "qualifiedTheorem": "Maha.Interval.add_valid",
      "claimIds": [
        "clm_interval_composition"
      ],
      "assumptions": [
        "Both operands are intervals whose lower bound does not exceed its upper bound.",
        "The quantities are represented as exact integers in the units declared by the calculation receipt."
      ],
      "informalBoundary": "Establishes only that bound-wise addition of two ordered integer intervals yields an ordered interval. It does not establish that the WASM kernel implements this operation, that the inputs describe any measured quantity, or that interval arithmetic is the correct uncertainty model for the cited figure.",
      "calculationOperationIds": [
        "interval-add"
      ]
    },
    {
      "bindingId": "bnd_interval_add_mem",
      "revision": 1,
      "dossierId": "dos_internal_interval_tolerance_fixture",
      "qualifiedTheorem": "Maha.Interval.add_mem",
      "claimIds": [
        "clm_interval_composition"
      ],
      "assumptions": [
        "Each operand contains its respective member.",
        "Quantities are exact integers in the declared units."
      ],
      "informalBoundary": "Establishes only that the sum of two members lies within the summed interval. It is a containment property of the arithmetic, not evidence about measurement error, instrument calibration, or the physical process the figure describes.",
      "calculationOperationIds": [
        "interval-add"
      ]
    }
  ]
}
