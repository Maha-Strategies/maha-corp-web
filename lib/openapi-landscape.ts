export const LANDSCAPE_OPT_RESEARCH_URL = 'https://research.mahastrategies.com/atlas/landscape-opt'
export type LandscapeHardware = 'gpu' | 'tpu'
export interface LandscapeJobRequest { clientRequestId: string; variables: { rows: number; columns: number; integer: boolean }; objective: { formulation: string; sense: 'minimize' | 'maximize' }; constraints: { equalities: string[]; inequalities: string[]; polynomialDegreeMax: number }; execution: { hardware: LandscapeHardware; maxWallTimeSeconds: number; maxMemoryGb: number } }
export interface LandscapeJobResponse { mock: true; jobId: string; status: 'queued'; clientRequestId: string; inputHash: string; criticalPointMap: { status: 'mock' }; convergenceTrajectory: { status: 'mock' }; citations: Array<{ claimId: string; url: string; role: 'method-basis'; verificationBoundary: 'research-node' }>; sourceInputStored: false }
const requestSchema = { type: 'object', required: ['clientRequestId', 'variables', 'objective', 'constraints', 'execution'], properties: { clientRequestId: { type: 'string', minLength: 8, maxLength: 120 }, variables: { type: 'object', required: ['rows', 'columns', 'integer'], properties: { rows: { type: 'integer', minimum: 1, maximum: 1000000000 }, columns: { type: 'integer', minimum: 1, maximum: 1000000000 }, integer: { type: 'boolean' } } }, objective: { type: 'object', required: ['formulation', 'sense'], properties: { formulation: { type: 'string', minLength: 1, maxLength: 16000 }, sense: { type: 'string', enum: ['minimize', 'maximize'] } } }, constraints: { type: 'object', required: ['equalities', 'inequalities', 'polynomialDegreeMax'], properties: { equalities: { type: 'array', maxItems: 10000, items: { type: 'string', maxLength: 16000 } }, inequalities: { type: 'array', maxItems: 10000, items: { type: 'string', maxLength: 16000 } }, polynomialDegreeMax: { type: 'integer', minimum: 0, maximum: 1000 } } }, execution: { type: 'object', required: ['hardware', 'maxWallTimeSeconds', 'maxMemoryGb'], properties: { hardware: { type: 'string', enum: ['gpu', 'tpu'] }, maxWallTimeSeconds: { type: 'integer', minimum: 1, maximum: 604800 }, maxMemoryGb: { type: 'number', exclusiveMinimum: 0, maximum: 1000000 } } } } } as const
export const landscapeOpenApiPath = { '/api/v1/landscape-opt': { post: { tags: ['Maha Landscape-Opt (Mock)'], operationId: 'createMockLandscapeOptJob', summary: 'Validate a high-dimensional landscape request and return a mock job', description: 'Integration stub only. It executes no solver, retains no formulation, and makes no convergence or optimality claim.', requestBody: { required: true, content: { 'application/json': { schema: requestSchema } } }, responses: { '202': { description: 'Mock landscape job accepted; no optimization has run.', headers: { 'X-Maha-API-Mode': { schema: { const: 'mock' } } }, content: { 'application/json': { schema: { type: 'object', required: ['mock', 'jobId', 'status', 'clientRequestId', 'inputHash', 'criticalPointMap', 'convergenceTrajectory', 'citations', 'sourceInputStored'], properties: { mock: { const: true }, jobId: { type: 'string', pattern: '^lso_[a-f0-9]{32}$' }, status: { const: 'queued' }, clientRequestId: { type: 'string' }, inputHash: { type: 'string', pattern: '^[a-f0-9]{64}$' }, criticalPointMap: { type: 'object' }, convergenceTrajectory: { type: 'object' }, citations: { type: 'array', minItems: 1, items: { type: 'object' } }, sourceInputStored: { const: false } } } } } }, '400': { description: 'Invalid landscape optimization mock request.' }, '415': { description: 'Content-Type must be application/json.' } } } } } as const
