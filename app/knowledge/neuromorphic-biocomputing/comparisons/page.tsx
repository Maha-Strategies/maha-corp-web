import type { Metadata } from 'next'
import Link from 'next/link'
import { SITE_URL } from '@/lib/briefs-data'
import { NEUROMORPHIC_COMPARISONS, NEUROMORPHIC_COMPARISONS_PATH, NEUROMORPHIC_PATH, neuromorphicComparisonPath } from '@/lib/neuromorphic-biocomputing'

export const metadata: Metadata = { metadataBase: new URL(SITE_URL), title: 'Substrate Comparisons | Neuromorphic and Biocomputing', description: 'Eight bounded comparisons for models, silicon, living substrates, hybrid systems, energy, learning, and deployment.', alternates: { canonical: NEUROMORPHIC_COMPARISONS_PATH } }
export default function ComparisonsPage() { return <main className="min-h-screen bg-[#080a0b] px-6 py-16 text-zinc-300 sm:px-12"><div className="mx-auto max-w-5xl"><nav className="font-mono text-[10px] uppercase tracking-widest text-zinc-600"><Link href={NEUROMORPHIC_PATH}>Neuromorphic and biocomputing</Link><span className="px-2">/</span><span className="text-zinc-400">Comparisons</span></nav><header className="mt-10 border-b border-zinc-800 pb-10"><p className="font-mono text-[10px] uppercase tracking-widest text-lime-300">Comparison corpus · substrate aware</p><h1 className="mt-6 text-4xl font-semibold text-white sm:text-6xl">Compare measurements without erasing what produced them.</h1><p className="mt-6 max-w-3xl font-serif text-lg leading-8 text-zinc-300">Each record freezes valid comparison axes, preserves non-equivalences, and names the inference that cannot be made.</p></header><div className="mt-10 grid gap-5 md:grid-cols-2">{NEUROMORPHIC_COMPARISONS.map((item) => <Link key={item.id} href={neuromorphicComparisonPath(item)} className="group border border-zinc-800 p-6 hover:border-lime-600/60"><p className="font-mono text-[9px] uppercase tracking-widest text-lime-300">{item.sides.length} substrates</p><h2 className="mt-3 text-xl font-semibold text-white group-hover:text-lime-200">{item.title}</h2><p className="mt-3 text-sm leading-6 text-zinc-500">{item.question}</p></Link>)}</div></div></main> }
