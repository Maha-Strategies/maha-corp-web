import { bearerApiKey, getApiKeyRecordForRawKey } from '@/lib/api-key'
export const runtime = 'nodejs'
export const dynamic = 'force-dynamic'
export async function GET(request: Request) { const key = bearerApiKey(request); if (!key) return Response.json({ error: { code: 'api_key_required', message: 'Provide Authorization: Bearer <API_KEY>.' } }, { status: 401 }); try { const record = await getApiKeyRecordForRawKey(key); if (!record || record.status !== 'active') return Response.json({ error: { code: 'invalid_api_key', message: 'This API key is invalid or inactive.' } }, { status: 401 }); return Response.json({ balance_credits: Number(record.balance_credits), tier: record.tier }, { headers: { 'Cache-Control': 'no-store' } }) } catch { return Response.json({ error: { code: 'api_key_service_unavailable', message: 'API authorization is temporarily unavailable.' } }, { status: 503 }) } }
